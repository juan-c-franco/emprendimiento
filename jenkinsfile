pipeline {
    agent any
    stages {
        stage('Git Checkout') {
            steps {
                git credentialsId: 'juancfranco', url: 'https://github.com/yonki/emprendimiento.git'
            }
        }
    }  
}
